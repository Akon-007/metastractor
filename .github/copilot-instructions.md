This repository contains a Streamlit metadata extractor app.

- `app.py` is the main application.
- `requirements.txt` lists Python dependencies.
- `README.md` includes setup and usage instructions.
- `.vscode/tasks.json` provides a run task for the Streamlit app.
